import { HeroContainer, HeroDisplay, HeroTitle } from './styles'

const Hero = () => (
  <HeroDisplay>
    <HeroContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContainer>
  </HeroDisplay>
)

export default Hero
